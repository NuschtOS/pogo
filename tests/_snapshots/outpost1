#pretty
{
  boot = {
    initrd = {
      luks = {
        devices = {
          nixos-root1 = {
            device = "/dev/disk/by-id/ata-SAMSUNG_MZ7LM480HCHP-00003_S1YJNX0H401022-part2";
            name = "nixos-root1";
          };
          nixos-root2 = {
            device = "/dev/disk/by-id/ata-SAMSUNG_MZ7LM480HMHQ-00005_S2UJNX0H801406-part2";
            name = "nixos-root2";
          };
        };
      };
    };
    loader = {
      grub = {
        mirroredBoots = [
          {
            devices = [
              "/dev/disk/by-id/ata-SAMSUNG_MZ7LM480HCHP-00003_S1YJNX0H401022"
            ];
            path = "/boot1";
          }
          {
            devices = [
              "/dev/disk/by-id/ata-SAMSUNG_MZ7LM480HMHQ-00005_S2UJNX0H801406"
            ];
            path = "/boot2";
          }
        ];
      };
    };
  };
  fileSystems = {
    "/" = {
      depends = [ ];
      device = "nixos-root/root";
      fsType = "zfs";
      mountPoint = "/";
      neededForBoot = false;
      options = [
        "x-initrd.mount"
        "defaults"
        "zfsutil"
      ];
    };
    "/etc" = {
      depends = [ ];
      device = "nixos-root/data/etc";
      fsType = "zfs";
      mountPoint = "/etc";
      neededForBoot = false;
      options = [
        "x-initrd.mount"
        "defaults"
        "zfsutil"
      ];
    };
    "/home" = {
      depends = [ ];
      device = "nixos-root/home";
      fsType = "zfs";
      mountPoint = "/home";
      neededForBoot = false;
      options = [
        "defaults"
        "zfsutil"
      ];
    };
    "/nix" = {
      depends = [ ];
      device = "nixos-root/nix";
      fsType = "zfs";
      mountPoint = "/nix";
      neededForBoot = false;
      options = [
        "x-initrd.mount"
        "defaults"
        "zfsutil"
      ];
    };
    "/nix/store" = {
      depends = [ ];
      device = "nixos-root/nix/store";
      fsType = "zfs";
      mountPoint = "/nix/store";
      neededForBoot = false;
      options = [
        "x-initrd.mount"
        "defaults"
        "zfsutil"
      ];
    };
    "/nix/var" = {
      depends = [ ];
      device = "nixos-root/nix/var";
      fsType = "zfs";
      mountPoint = "/nix/var";
      neededForBoot = false;
      options = [
        "defaults"
        "zfsutil"
      ];
    };
    "/var" = {
      depends = [ ];
      device = "nixos-root/data/var";
      fsType = "zfs";
      mountPoint = "/var";
      neededForBoot = false;
      options = [
        "x-initrd.mount"
        "defaults"
        "zfsutil"
      ];
    };
    "/var/backup" = {
      depends = [ ];
      device = "nixos-root/data/var/backup";
      fsType = "zfs";
      mountPoint = "/var/backup";
      neededForBoot = false;
      options = [
        "defaults"
        "zfsutil"
      ];
    };
    "/var/lib" = {
      depends = [ ];
      device = "nixos-root/data/var/lib";
      fsType = "zfs";
      mountPoint = "/var/lib";
      neededForBoot = false;
      options = [
        "x-initrd.mount"
        "defaults"
        "zfsutil"
      ];
    };
    "/var/log" = {
      depends = [ ];
      device = "nixos-root/data/var/log";
      fsType = "zfs";
      mountPoint = "/var/log";
      neededForBoot = false;
      options = [
        "x-initrd.mount"
        "defaults"
        "zfsutil"
      ];
    };
  };
  swapDevices = [ ];
}