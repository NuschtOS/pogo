#pretty
{
  boot = {
    initrd = {
      luks = {
        devices = {
          synthetic-glotzbert = {
            device = "/dev/disk/by-id/ata-SSD0240S00_20201124BC41037-part2";
            name = "synthetic-glotzbert";
          };
        };
      };
    };
    loader = {
      grub = {
        mirroredBoots = [ ];
      };
    };
  };
  disko = {
    devices = {
      disk = {
        "/dev/disk/by-id/ata-SSD0240S00_20201124BC41037" = {
          content = {
            partitions = [
              {
                end = "1Gib";
                start = "1MiB";
              }
              {
                end = "100%";
                start = "1Gib";
              }
            ];
          };
        };
      };
    };
  };
  fileSystems = {
    "/" = {
      depends = [ ];
      device = "synthetic-glotzbert/root";
      fsType = "zfs";
      mountPoint = "/";
      neededForBoot = false;
      options = [
        "x-initrd.mount"
        "defaults"
        "zfsutil"
      ];
    };
    "/boot" = {
      depends = [ ];
      device = "/dev/disk/by-id/ata-SSD0240S00_20201124BC41037-part1";
      fsType = "vfat";
      mountPoint = "/boot";
      neededForBoot = false;
      options = [
        "defaults"
      ];
    };
    "/etc" = {
      depends = [ ];
      device = "synthetic-glotzbert/data/etc";
      fsType = "zfs";
      mountPoint = "/etc";
      neededForBoot = false;
      options = [
        "x-initrd.mount"
        "defaults"
        "zfsutil"
      ];
    };
    "/home" = {
      depends = [ ];
      device = "synthetic-glotzbert/home";
      fsType = "zfs";
      mountPoint = "/home";
      neededForBoot = false;
      options = [
        "defaults"
        "zfsutil"
      ];
    };
    "/nix" = {
      depends = [ ];
      device = "synthetic-glotzbert/nix";
      fsType = "zfs";
      mountPoint = "/nix";
      neededForBoot = false;
      options = [
        "x-initrd.mount"
        "defaults"
        "zfsutil"
      ];
    };
    "/nix/store" = {
      depends = [ ];
      device = "synthetic-glotzbert/nix/store";
      fsType = "zfs";
      mountPoint = "/nix/store";
      neededForBoot = false;
      options = [
        "x-initrd.mount"
        "defaults"
        "zfsutil"
      ];
    };
    "/nix/var" = {
      depends = [ ];
      device = "synthetic-glotzbert/nix/var";
      fsType = "zfs";
      mountPoint = "/nix/var";
      neededForBoot = false;
      options = [
        "defaults"
        "zfsutil"
      ];
    };
    "/var" = {
      depends = [ ];
      device = "synthetic-glotzbert/data/var";
      fsType = "zfs";
      mountPoint = "/var";
      neededForBoot = false;
      options = [
        "x-initrd.mount"
        "defaults"
        "zfsutil"
      ];
    };
    "/var/backup" = {
      depends = [ ];
      device = "synthetic-glotzbert/data/var/backup";
      fsType = "zfs";
      mountPoint = "/var/backup";
      neededForBoot = false;
      options = [
        "defaults"
        "zfsutil"
      ];
    };
    "/var/lib" = {
      depends = [ ];
      device = "synthetic-glotzbert/data/var/lib";
      fsType = "zfs";
      mountPoint = "/var/lib";
      neededForBoot = false;
      options = [
        "x-initrd.mount"
        "defaults"
        "zfsutil"
      ];
    };
    "/var/log" = {
      depends = [ ];
      device = "synthetic-glotzbert/data/var/log";
      fsType = "zfs";
      mountPoint = "/var/log";
      neededForBoot = false;
      options = [
        "x-initrd.mount"
        "defaults"
        "zfsutil"
      ];
    };
  };
  swapDevices = [ ];
}